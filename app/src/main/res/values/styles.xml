<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Widget.OpenUserAgent.HeaderBar.Spinner" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@null</item>
        <item name="android:overlapAnchor" tools:targetApi="lollipop">true</item>
        <item name="android:listPreferredItemHeight">48dp</item>
        <item name="android:dropDownItemStyle">@style/Widget.OpenUserAgent.Menu.item</item>
        <item name="android:listPreferredItemPaddingLeft">16dp</item>
        <item name="android:paddingBottom">20dp</item>
    </style>
    <style name="Widget.OpenUserAgent.Menu.item" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:listPreferredItemHeight">48dp</item>
        <item name="android:listPreferredItemPaddingLeft">16dp</item>
        <item name="android:paddingBottom">20dp</item>
    </style>
</resources>
